Import('env')

headers = [
    'phui',
    'Button',
    'RootWidget',
    'WidgetContainer',
    'Widget',
    'Window',
]

sources = [    
]

# add proper extensions to filenames
headers = map(lambda fn: fn + '.h', headers)
sources = map(lambda fn: fn + '.cpp', sources)

env.Library(target = 'phui', source = headers + sources)
